[plugin]
name        = "rudra-share"
version     = "0.1.0"
description = "Instant zero-config public tunneling and port sharing — expose local dev servers to the internet with QR codes"
author      = "Mystery3589"
rudra_min   = "0.1.0"
homepage    = "https://github.com/Mystery3589/rudra-plugins"
license     = "MIT"
commands    = ["share", "tunnel"]

[dependencies]
python = []
